Notes

AI Assisted Programing: VSCode with Copilot
4/27/2025
- accepted the changes to the varMeans function because going through one loop was better
- accepted changes to the gradient function, which now has more idiomatic code
- the changes to the yIntercept function were basically to just add comments. In the text of the chatlog, it said it was removing redundant calls to varMeans and gradient, but that wasn't what was displayed in the text. I did accept the changes(ie. adding comments), but they weren't as substantial as the other changes. 
- i did like the improvements made to the main function. will have to create a test function for the new function though.
- when i asked the chat agent if my approach was the best way to calculate linear regression coefficients, it recommended combining the gradient and y-intercept functions. i would prefer not to do this because i think the function would be doing too much at once but i could also see why the agent would recommend it.
- provided additional packages that could be used for linear regression coefficients. i think the current package is fine because it is a simple calculation and the stats package was specified in the assignment.

- i did benchmark the code that copilot recommmends for the stats package, and my code original code is faster

- kept the chat history in mind when i started asking questions about benchmarking and testing, so had to specify that i only wanted tests and benchmarks for the code that was currently there
- created new tests that filled in gaps in my current tests. i especially liked the tests that showed how to check for nil values when we expect them. some the test codes seemed a little complex. i think it might be easier to refactor the code so that the tests are handled in the main program instead.


AI Assisted Programming: VSCode with Copilot
4/29/2025
-shifted over to asking GitHub Copilot to look over the Python code
- gave three answers, only the first was relevant. did give a good background for why each version could be a good option.
- time for python code without loop is 0.5 sec
- time with Copilot recommendation is (the for loop) is 0.1 sec
- asked similar questions for the R program
- time to take to run the regular program without Copilot suggestions what roughly 0.02 sec
- only decided to take the for-loop suggestion because the other ones weren't needed
- the for loop also took roughly 0.02 sec to run, but appeared to be slower than the other version

AI Generated Code: Chat GPT
5/1/2025
- decided to see how ChatGPT performs
- first tested to see if it could give information about the dataset without me inputting the information directly
- testing the resulting code
- gave a version that was able to mostly do the calculation, but gave NaNs for the last dataset
- didn't use the package from the brief
- when I asked it to re-run the program given that it did not have the correct dataset values, it moved everything into the main function
- takes roughly .505 sec for the program to run
- asked it to refactor the code to create a program with functions instead of everything in the main function
- new refactored code takes 0.613 sec
- generated new python code
- chatGPT did all of it with numpy, instead of pandas or scikit
- performed well? (8182 micro-sec)
- asked ChatGPT to do the same things for R
- R program generated by ChatGPT took 0.0217 sec to run

